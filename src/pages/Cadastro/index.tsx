import FormCadastro from "container/Formulario";
import Sidebar from "container/SideBar";

const Cadastro = () => (
    <>
    <Sidebar showFilter={false} />
    <FormCadastro />
  </>
);

export default Cadastro;
